import { TextEditor } from '@99draft/editor-react';

function App() {
  return (
    <main className="bg-black/5 w-full h-screen flex items-center justify-center">
      <TextEditor />
    </main>
  );
}

export default App;
